# 🔍 快速診斷指南

## ❓ 如果網頁無法顯示資料

### 步驟 1: 打開 Console（最重要）

1. 打開網站
2. 按 `F12` 打開開發者工具
3. 前往 **Console** 標籤
4. **複製所有 Console 輸出**給我

### 步驟 2: 檢查 Firebase 配置

在 Console 中應該看到：
```
🔧 [Firebase] 配置檢查:
  - API Key: ✅ 已設定
  - Project ID: ...
  - Auth Domain: ✅ 已設定
  ...
```

**如果有 ❌ 未設定**:
- 前往 Vercel Dashboard > Settings > Environment Variables
- 確認所有 `VITE_` 開頭的變數已設定

### 步驟 3: 檢查 Firestore 連接

在 Console 中應該看到：
```
✅ [Firebase] Firestore 連接成功
🔍 [useNewsData] 開始讀取 Firestore...
📊 [useNewsData] 收到快照，文件數: X
✅ [useNewsData] 成功載入 X 筆資料
```

**如果看到錯誤**:
- `Missing or insufficient permissions` → 檢查 Firestore 規則
- `資料庫是空的` → 確認 Railway 後端已執行

### 步驟 4: 檢查 Firestore 資料

1. 前往 [Firebase Console](https://console.firebase.google.com/)
2. Firestore Database
3. 確認 `daily_news` collection 有文件
4. 確認文件包含 `content` 欄位

---

## 🆘 常見錯誤與解決方案

### 錯誤 1: "API Key: ❌ 未設定"

**解決**:
1. Vercel Dashboard > Settings > Environment Variables
2. 新增 `VITE_FIREBASE_API_KEY`
3. 重新部署

### 錯誤 2: "Missing or insufficient permissions"

**解決**:
1. Firebase Console > Firestore Database > Rules
2. 更新規則：
```javascript
match /daily_news/{document=**} {
  allow read: if true;
}
```

### 錯誤 3: "資料庫是空的"

**解決**:
1. 確認 Railway 後端已執行
2. 檢查 Railway Logs 是否有錯誤
3. 確認 Firestore 有資料

---

## 📋 請提供以下資訊

如果還是有問題，請提供：

1. **Console 完整輸出**（F12 > Console，全部複製）
2. **Network 請求狀態**（F12 > Network，查看 Firebase 相關請求）
3. **Firestore 資料截圖**（Firebase Console）
4. **Vercel Build Logs**（如果有錯誤）

這樣我可以更準確地診斷問題！

