# 🚀 首次部署完整步驟

## 步驟 1: 在 GitHub 建立 Repository

### 1.1 建立新的 GitHub Repository

1. 前往 https://github.com/new
2. 登入您的 GitHub 帳號
3. 填寫以下資訊：
   - **Repository name**: `ai-news-worker`（或您喜歡的名稱）
   - **Description**: AI News Worker - 自動化新聞分析系統（可選）
   - **Visibility**: 選擇 Public 或 Private
   - **⚠️ 重要**: 不要勾選 "Initialize this repository with a README"（因為我們已經有檔案了）
4. 點擊 "Create repository"

### 1.2 複製 Repository URL

建立完成後，GitHub 會顯示 Repository URL，格式類似：
```
https://github.com/你的用戶名/ai-news-worker.git
```

**請記下這個 URL，接下來會用到！**

---

## 步驟 2: 推送到 GitHub

### 2.1 連接遠端 Repository

在終端機執行以下命令（將 `你的用戶名` 替換為您的 GitHub 用戶名）：

```bash
cd /Users/yveschen/Desktop/ai-news-worker

# 連接遠端 repository
git remote add origin https://github.com/你的用戶名/ai-news-worker.git

# 確認連接成功
git remote -v
```

### 2.2 推送到 GitHub

```bash
# 確保所有檔案都已提交
git add -A
git commit -m "Ready for deployment"

# 推送到 GitHub
git branch -M main
git push -u origin main
```

**注意**: 第一次推送時，GitHub 可能會要求您輸入帳號密碼或使用 Personal Access Token。

如果遇到認證問題，可以：
- 使用 GitHub CLI: `gh auth login`
- 或使用 Personal Access Token（見下方說明）

---

## 步驟 3: 在 Vercel 部署前端

### 3.1 建立 Vercel 專案

1. 前往 https://vercel.com/
2. 點擊 "Sign Up" 或 "Log In"
3. 選擇 "Continue with GitHub" 使用 GitHub 帳號登入
4. 授權 Vercel 存取您的 GitHub repositories

### 3.2 匯入專案

1. 在 Vercel Dashboard 點擊 "Add New Project"
2. 選擇 "Import Git Repository"
3. 找到並選擇 `ai-news-worker` repository
4. 點擊 "Import"

### 3.3 設定專案

在專案設定頁面：

1. **Framework Preset**: 選擇 `Vite`（Vercel 通常會自動偵測）

2. **Root Directory**: 
   - 點擊 "Edit"
   - 輸入: `frontend`
   - 點擊 "Continue"

3. **Environment Variables**（重要！）:
   - 點擊 "Environment Variables"
   - 新增以下變數（每個都要新增）：
     ```
     VITE_FIREBASE_API_KEY = 你的_api_key
     VITE_FIREBASE_AUTH_DOMAIN = 你的專案.firebaseapp.com
     VITE_FIREBASE_PROJECT_ID = 你的專案_id
     VITE_FIREBASE_STORAGE_BUCKET = 你的專案.appspot.com
     VITE_FIREBASE_MESSAGING_SENDER_ID = 你的_sender_id
     VITE_FIREBASE_APP_ID = 你的_app_id
     ```
   - 每個變數都要選擇環境（Production, Preview, Development）
   - 點擊 "Save"

4. **Build and Output Settings**:
   - Build Command: `npm run build`（通常會自動偵測）
   - Output Directory: `dist`（通常會自動偵測）

5. 點擊 "Deploy"

### 3.4 等待部署完成

- Vercel 會自動開始建置和部署
- 通常需要 1-2 分鐘
- 部署完成後會顯示一個 URL，例如：`https://ai-news-worker.vercel.app`

---

## 步驟 4: 在 Railway 部署後端

### 4.1 建立 Railway 專案

1. 前往 https://railway.app/
2. 點擊 "Start a New Project" 或 "Login"
3. 選擇 "Deploy from GitHub repo"
4. 授權 Railway 存取您的 GitHub
5. 選擇 `ai-news-worker` repository

### 4.2 設定服務

1. **Railway 會自動偵測 Dockerfile**
   - 確認服務名稱（可以保持預設或修改）
   - 確認 Root Directory 設為 `backend`（如果沒有自動偵測）

2. **設定環境變數**:
   - 在服務頁面，點擊 "Variables" 標籤
   - 點擊 "New Variable"
   - 新增：
     ```
     Key: OPENAI_API_KEY
     Value: 你的_openai_api_key
     ```
   - 點擊 "Add"

3. **上傳服務帳號金鑰**:
   - 在服務頁面，點擊 "Settings" 標籤
   - 找到 "Secrets" 區塊
   - 點擊 "New Secret"
   - 名稱: `SERVICE_ACCOUNT_KEY`
   - 值: 貼上 `backend/serviceAccountKey.json` 的完整 JSON 內容
     ```bash
     # 在終端機查看內容
     cat backend/serviceAccountKey.json
     ```
   - 點擊 "Add Secret"

### 4.3 確認部署

1. Railway 會自動開始部署
2. 查看 "Deployments" 標籤確認部署狀態
3. 查看 "Logs" 標籤確認服務運行狀況
4. 應該會看到：
   ```
   ⏰ AI News Worker 排程器已啟動
   📅 下次執行時間: ...
   ```

---

## ✅ 部署完成檢查

### 前端檢查

1. 訪問 Vercel 提供的 URL
2. 打開瀏覽器開發者工具（F12）> Console
3. 確認沒有錯誤訊息
4. 如果看到 "資料庫是空的" 是正常的（後端還沒執行任務）

### 後端檢查

1. 在 Railway Dashboard 查看 Logs
2. 確認看到排程器啟動訊息
3. 等待排程時間或手動測試（見下方）

---

## 🧪 測試後端（可選）

如果想立即測試後端是否正常：

1. 在 Railway 服務頁面，點擊 "Settings"
2. 找到 "Deploy" 區塊
3. 可以重新部署來觸發執行
4. 或等待到設定的執行時間（預設每天 09:00 UTC）

---

## 🔐 GitHub Personal Access Token（如果需要）

如果推送時遇到認證問題：

1. 前往 https://github.com/settings/tokens
2. 點擊 "Generate new token" > "Generate new token (classic)"
3. 填寫：
   - Note: `Git CLI`
   - Expiration: 選擇期限
   - Scopes: 勾選 `repo`
4. 點擊 "Generate token"
5. 複製 token（只會顯示一次！）
6. 使用 token 作為密碼推送：
   ```bash
   git push -u origin main
   # Username: 你的 GitHub 用戶名
   # Password: 貼上剛才複製的 token
   ```

---

## 📝 快速命令參考

```bash
# 檢查 Git 狀態
git status

# 檢查遠端連接
git remote -v

# 推送到 GitHub
git push -u origin main

# 查看服務帳號金鑰內容（用於 Railway）
cat backend/serviceAccountKey.json
```

---

## 🆘 遇到問題？

### GitHub 推送失敗
- 確認 Repository URL 正確
- 確認已登入 GitHub
- 使用 Personal Access Token

### Vercel 部署失敗
- 檢查環境變數是否全部設定
- 確認 Root Directory 設為 `frontend`
- 查看 Vercel 的 Build Logs

### Railway 部署失敗
- 檢查環境變數是否設定
- 確認 SERVICE_ACCOUNT_KEY Secret 格式正確（完整 JSON）
- 查看 Railway 的 Logs

---

祝部署順利！🎉

